# Define an anonymous function to calculate the area of a square
calculate_area = lambda side: side ** 2

# Test the function
side_length = 5
area = calculate_area(side_length)
print("Area of the square with side length", side_length, "is", area)

